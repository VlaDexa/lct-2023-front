export type Group = {
    id: string,
    name: string,
    tags: [string, string, string]
    metro: string,
    timeToWalk: number,
    time: string,
}

export default function GroupCard(props: {group: Group}) {
    return <div>

    </div>
}